{% extends "base.html.twig" %}

{% block nav %}{% endblock %}

{% block body %}
	<div class="MoviePlayer">
		<div class="MoviePlayer-cover">
			<img src="{{movie.cover}}" alt="{{movie.title}}">
		</div>
		<div class="MoviePlayer-body">
			<div class="content">
				<h1>{{movie.title}}</h1>
				<div class="spinner-border" role="status">
					<span class="visually-hidden">Loading...</span>
				</div>
				<p>Chargement du film ...</p>
				<a class="btn btn-outline-danger" href="/">Retour à l'accueil</a>
			</div>
		</div>
	</div>
{% endblock %}
